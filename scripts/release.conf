# La version la consume el aviso de "hay version nueva" (useVersionCheck),
# asi que manda package.json, no la fecha.
SCHEME=semver
VERSION_FILE=package.json
DEPLOY_PUSH=""   # el push a origin main dispara el deploy
